using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class deneme : MonoBehaviour
{

    [SerializeField]
    private GameObject Deneme;
    void Start()
    {
        Deneme.SetActive(false);
    }

    // Update is called once per frame
    
}
